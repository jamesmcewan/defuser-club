pipeline:
  docker:
    image: plugins/docker
    repo: jamesvmcewan/mce-react
    username: docker_username
    password: docker_password
    autotag: true
    secrets:
      - docker_username
      - docker_password
